# Vanilla-JS-Word-Counter

This is just a simple character/word counter app made with just HTML, CSS, and JavaScript. It uses string methods such as `split` and higher order array methods such as `filter`.
